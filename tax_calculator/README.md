# coding-project-template

IBM Tax Calculator final project (JavaScript track).

- Jasmine tests: `npm install` then `npx jasmine` (7 specs, 0 failures)
- Docker image: `docker build -t tax-calculator .`
- Run locally: `docker run -d -p 8080:80 tax-calculator`
